<ui:FluentWindow x:Class="ScarpaConnectionManager.Dialogs.TextInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Input" Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True">
    
    <DockPanel LastChildFill="True">
        <!-- Native Windows 11 Title Bar -->
        <ui:TitleBar x:Name="DialogTitleBar" Title="Input" DockPanel.Dock="Top" />

        <StackPanel Margin="18">
            <TextBlock x:Name="PromptText" TextWrapping="Wrap" Margin="0,0,0,10" />
            <TextBox x:Name="Input" Padding="4" />
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="Cancel" IsCancel="True" Click="Cancel_Click" Margin="0,0,8,0" Width="70" />
                <Button Content="OK" IsDefault="True" Click="Ok_Click" Width="70" />
            </StackPanel>
        </StackPanel>
    </DockPanel>
</ui:FluentWindow>